const isNaN = (object) => {
    // NaN and only NaN compares unequal to itself
    return object !== object
}

module.exports = {
    isNaN
}

let isNaN_Output = isNaN([1])

    ([]) == ([])
var object = [];
object == object

var a = 2
process(a);
a++;

function process(b) {
    b++;
}
console.log(a)